{
  "name": "coredrop-nft-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only NFT gallery for Core Blockchain (curated collections)",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  }
}
